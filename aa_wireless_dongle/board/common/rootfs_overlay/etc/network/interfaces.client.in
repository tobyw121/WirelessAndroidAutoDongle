auto lo
iface lo inet loopback

auto @WIFI_INTERFACE@
iface @WIFI_INTERFACE@ inet dhcp
  pre-up wpa_supplicant -D nl80211 -i @WIFI_INTERFACE@ -c /var/run/wpa_supplicant.conf -B
  post-down killall -q wpa_supplicant
